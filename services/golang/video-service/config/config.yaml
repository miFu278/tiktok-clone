service:
  name: video-service
  environment: development

server:
  grpc_port: 50051

postgres:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  dbname: tiktok_videos
  sslmode: disable

redis:
  addr: localhost:6379

storage:
  type: s3
  bucket: tiktok-videos
  region: us-east-1

transcoding:
  enabled: true
  workers: 4
